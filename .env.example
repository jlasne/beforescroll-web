# Copy to .env.local and fill in real values. Never commit real secrets.
# Production values live in Vercel project env vars.

# Supabase — project URL + keys from dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Loops — transactional email + marketing sequences
LOOPS_API_KEY=

# RevenueCat — shared secret used to verify webhook Authorization: Bearer <...>
REVENUECAT_WEBHOOK_SECRET=
